export interface User {
    id: number;
    name: string;
    email: string;
    createdAt: Date;
}

export interface Item {
    id: number;
    name: string;
    price: number;
    createdAt: Date;
}